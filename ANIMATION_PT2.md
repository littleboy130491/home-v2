when user scroll to section "social-proof" the section will turn into light theme. the animation should looks luxurious. btw, we use class called ".section-dark" to turn the background color.
when user scroll to section "home-cta" there will be countdown JS from 3 to 1. after it's finished, change section to dark.
